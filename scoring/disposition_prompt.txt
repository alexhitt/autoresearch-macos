You are classifying research claims for an AI automation system (the "AI OS").

__SYSTEM_CONTEXT__

IMPORTANT: Before classifying any claim as "apply_now", check the existing rules list above. If an existing rule already covers what the claim recommends, classify as "discard" with reason "already covered by {rule name}". Only classify as "apply_now" when the claim describes something genuinely NEW that the system does not already do.

CLASSIFICATION BIAS: When a claim is relevant to an active project or existing system, default toward "apply_now" unless there is a clear reason not to. The cost of missing an actionable claim is higher than the cost of surfacing one that turns out to be low priority. Ask: "Could the owner act on this within existing systems?" If yes, it is apply_now.

For each claim, assign a disposition:
- "apply_now": the claim is relevant to something the AI OS already does or is actively building, AND describes a concrete improvement, technique, or finding. Specifically, classify as apply_now if ANY of these apply:
  (a) A security vulnerability, fix pattern, or defensive technique — even if discovered in another codebase — that could apply to our systems (path traversal, injection, silent error swallowing, unbounded data structures, etc.)
  (b) A new SDK version, API capability, or tool feature that an existing system could use
  (c) A validated technique, strategy, or architecture directly relevant to an active project (prediction markets, memory systems, content generation, evaluation pipelines, agent orchestration)
  (d) A finding that reveals how to improve accuracy, reduce bias, or fix a known limitation in an existing system
  (e) A concrete implementation pattern (event extraction, memory layering, claim verification, resource bounding) that maps onto a system we already run
  (f) A finding about evaluation methodology relevant to how we assess our own systems
  Do NOT classify as apply_now if: it targets a domain we have no system for, describes a general best practice already followed, is about the pipeline's own commits, or would require building something with no existing foundation.
- "candidate_insight": genuinely novel but does NOT connect to any current system or active project. Interesting for future direction only. If you can name a specific existing system it would improve, it is apply_now, not candidate_insight.
- "discard": not actionable, too vague, no clear application, OR already covered by an existing rule

For ALL claims, regardless of disposition, provide:
- "reason": one sentence explaining why this matters to the AI OS in plain language
- "whyYes": 1-2 sentences — the strongest argument FOR acting on this claim. Be specific: what capability or improvement would it enable?
- "signalStrength": integer 1-10 — how much would acting on this claim benefit the AI OS owner? 10 = transformative leverage, critical security, or direct revenue impact. 5 = useful improvement. 1 = marginally interesting.

For "apply_now" claims, also provide a full understanding briefing in plain English:
- "actionSummary": one sentence — what to change, written as an instruction
- "targetSurface": one of "rules", "skills", "code", "memory", "architecture", "process"
- "impactLevel": one of "tweak" (change a few lines), "extend" (add something new), "rethink" (reconsider an approach)
- "whatThisIs": 1-2 sentences. What did the paper find or propose? No jargon.
- "whatItMeans": 1-2 sentences. How does this connect to our AI OS specifically? Name the specific system.
- "whatChanges": 1-3 sentences. What files, systems, or behaviors would change?
- "whyNot": 1-2 sentences. Strongest reason to skip this? Be honest.

Paper abstract:
__ABSTRACT__

Claims:
__CLAIM_LIST__

For each claim, include "claimId" matching the [id: ...] from above.
Respond with JSON array only. No commentary.