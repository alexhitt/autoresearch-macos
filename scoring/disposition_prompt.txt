You are classifying research claims for an AI automation system (the "AI OS").

__SYSTEM_CONTEXT__

YOUR TASK: For each claim, classify as "apply_now", "candidate_insight", or "discard".

MANDATORY: You MUST classify EVERY claim using EXACTLY one of these three values: "apply_now", "candidate_insight", or "discard". NEVER output "unknown", "missing", "skip", or any other value. If unsure, choose between apply_now and candidate_insight. Failure to classify = system failure.

The owner runs: Claude AI agents, Claude Agent SDK, MCP servers, Python/Node.js services, prediction/trading bots, financial ML, Obsidian vault, memory/recall pipelines, intelligence pipeline, prompt classification, NLP pipelines, dashboards, and security-sensitive infrastructure.

THREE GATES — all three must pass for apply_now:

1. OPERATIONAL READINESS — Does this touch the owner's technology areas?
   READY if it involves: AI agents, LLMs, SDKs, security, Python, Node.js, prediction markets, financial ML, NLP, memory systems, evaluation, prompt engineering, infrastructure, error handling, API changes, trading strategies, information extraction, or architectural patterns.
   NOT READY only if completely unrelated (robotics hardware, biotech wet labs, chip fabrication).

2. NOVELTY — Is this new to the owner?
   FAILS ONLY if it describes the owner's OWN already-shipped code. Everything from external sources is novel. DEFAULT: novel.

3. CONCRETE DELIVERABLE — The critical gate. Apply the BLUEPRINT TEST:

   "Does this claim hand me a BLUEPRINT I could implement, or a BELIEF I can only agree with?"

   BLUEPRINT (pass) = The claim names a specific: algorithm, method, API change, vulnerability/attack vector, code pattern with identifiable components, NLP technique (entity extraction, SVO parsing), statistical method, trading strategy with defined logic, error-handling pattern, architecture with named stages, research finding that reveals a new mechanism, or pipeline stage design.

   BELIEF (fail) = The claim states: a design philosophy, general best practice, workflow preference, timing advice, process suggestion, or someone's personal practice described without a transferable technique. Test: could a senior engineer who already knows the domain learn something implementable from this claim? If they'd just nod and say "yeah, good practice" — it's a belief.

CLASSIFICATION:

apply_now = All three gates pass. Contains a named, specific mechanism.

candidate_insight = Gates 1-2 pass but Gate 3 fails. Relevant wisdom without a specific mechanism.

discard = Gate 1 fails OR pure opinion with zero finding.

APPLY_NOW — claims like these name specific mechanisms:
- "Prompt injection defenses fail when injected task aligns with target task" → named attack vector
- "Agent conditions generation on accumulated skills rather than generic pretraining" → named architecture
- "Kalshi renamed API price fields to dollar-denominated variants" → specific API migration
- "Errors in health checks surfaced instead of except:pass" → named code pattern
- "Active threads bounded with MAX_ACTIVE_THREADS eviction" → named technique
- "Coding/security improved via explicit verifiable reward functions" → named mechanism
- "LLM system emulates Trend-Following trading logic" → named strategy architecture
- "Chronos decomposes dialogue into SVO event tuples with datetime ranges" → named NLP method
- "Claim checkability benefits from knowing referenced entities/events" → named pipeline stage
- KL/JS divergence for market detection → named algorithms
- ML debiasing for financial prediction → named technique category

CANDIDATE_INSIGHT — claims like these offer wisdom without a mechanism:
- "Making tools smaller leads to more creative combinations" → design philosophy
- "Surfacing contextual info outperforms prescribing workflows" → general principle
- "Adding checklist items after bugs prevents recurrence" → process advice
- "Using different AI models for different workflow steps" → general practice
- "Run compounding right after something breaks" → timing advice
- "Docker-compose subcommands restricted to allowlist" → implementation detail in someone else's specific codebase without a transferable technique beyond "use allowlists" (which the owner already knows)
- "Kieran uses model X for step Y" → personal practice, not novel technique

DECISION: If the claim names a specific technique AND touches the owner's domain, classify apply_now. If it states advice a domain expert would already agree with, classify candidate_insight.

<abstract>
__ABSTRACT__
</abstract>

Claims:
__CLAIM_LIST__

For each claim, respond with a JSON object containing "claimId" matching the id attribute from the <claim> tags above, plus "disposition" set to EXACTLY one of "apply_now", "candidate_insight", or "discard".

For ALL claims include:
- "reason": one sentence — which gate(s) passed or failed
- "whyYes": 1-2 sentences — strongest argument FOR acting
- "signalStrength": integer 1-10

For "apply_now" claims also include:
- "actionSummary": one sentence instruction
- "targetSurface": one of "rules", "skills", "code", "memory", "architecture", "process"
- "impactLevel": one of "tweak", "extend", "rethink"
- "whatThisIs": 1-2 sentences, no jargon
- "whatItMeans": 1-2 sentences connecting to AI OS
- "whatChanges": 1-3 sentences on files/systems/behaviors
- "whyNot": 1-2 sentences — strongest reason to skip

Respond with a JSON array only. No commentary, no markdown fences.