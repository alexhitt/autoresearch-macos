---
type: note
status: active
domain: autoresearch-pro
created: 2026-04-13
updated: 2026-04-13
complexity: low
pre_plan_ref: pre-plan-2026-04-13.md
trust_state: working
---

# Plan: Research-Guided Disposition Optimizer

## Goal

Add a research phase to `optimize_disposition.py` so the proposer receives targeted prompt engineering guidance based on current failure patterns, plus fix the 59% validation failure rate with constraint hardening and char budget awareness.

## Scope

- **In:** `scoring/optimize_disposition.py` (new `research()` function, modified `propose()`, modified `main()`)
- **Out:** `evaluate_disposition.py`, `build_system_context.py`, `disposition_prompt.txt`, overnight shell wrapper, launchd plists

## Steps

1. [x] Add `research()` function — calls `claude --print` with targeted query based on misclassified claims
2. [x] Add char budget + prompt skeleton to `propose()` — shows current length vs max, required structure
3. [x] Inject research findings into `propose()` — RESEARCH CONTEXT section
4. [x] Wire research into `main()` — call once after baseline, pass to all propose() calls
5. [ ] Test with `--dry-run` — verify research output, proposal validity, constraint awareness

## Acceptance

- [ ] `--dry-run` shows research output, proposal passes validation
- [ ] Proposer prompt includes char budget, skeleton, and research context
- [ ] Research failure doesn't crash the optimizer (graceful fallback)
- [ ] No changes to evaluate_disposition.py or the prompt file itself
- [ ] Full run produces at least 1 valid proposal in first 5 iterations
